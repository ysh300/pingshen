
server:
  # 服务器的HTTP端口，默认为80
  port: 8090
#数据库相关配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pingshen
    username: root
    password: '123678'

mybatis:
  configuration:
    #    指定输出到控制台
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    #    开启两种命名格式的匹配
    map-underscore-to-camel-case: true